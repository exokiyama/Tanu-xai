import 'dotenv/config';
import { PERMANENT_OWNERS } from './types.js';
const number = (value: string | undefined, fallback: string) => (value && /^\d{7,20}$/.test(value.replace(/\D/g, '')) ? value.replace(/\D/g, '') : fallback);
const TRUSTED_OWNER_NUMBER = PERMANENT_OWNERS[0].number; const TRUSTED_WIFE_NUMBER = PERMANENT_OWNERS[1].number; const MAX_RECONNECT_ATTEMPTS = 10;
export const config = Object.freeze({ nodeEnv: process.env.NODE_ENV ?? 'production', sessionId: process.env.SESSION_ID ?? '', ownerNumber: TRUSTED_OWNER_NUMBER, wifeNumber: TRUSTED_WIFE_NUMBER, botName: process.env.BOT_NAME ?? 'Tanu XAI', prefix: process.env.PREFIX ?? '.', mode: process.env.BOT_MODE === 'private' ? 'private' as const : 'public' as const, watermark: process.env.WATERMARK ?? 'Made by Arman HTX', packname: process.env.STICKER_PACKNAME ?? 'Tanu XAI', author: process.env.STICKER_AUTHOR ?? 'Arman HTX', repositoryUrl: process.env.REPOSITORY_URL ?? 'https://github.com/exokiyama/Tanu-xai', websiteUrl: process.env.WEBSITE_URL ?? '', channelUrl: process.env.CHANNEL_URL ?? '', supportUrl: process.env.SUPPORT_URL ?? '', reportEmail: process.env.REPORT_EMAIL_TO ?? process.env.REPORT_EMAIL ?? '', reportTime: process.env.REPORT_TIME ?? '00:00', databaseUrl: process.env.DATABASE_URL?.trim() || './data/tanu-xai.db', maxReconnectAttempts: MAX_RECONNECT_ATTEMPTS, dailyReportEnabled: process.env.DAILY_REPORT_ENABLED === 'true' });
export const phoneFromJid = (jid: string) => jid.split('@')[0].replace(/\D/g, '');
export const normalizePhone = (value: string | undefined) => number(value, TRUSTED_OWNER_NUMBER);
export function validateConfig() { if (!config.sessionId) throw new Error('SESSION_ID is required'); if (!config.sessionId.startsWith('Tanu-XAI~')) throw new Error('SESSION_ID must use Tanu-XAI~ format'); }
