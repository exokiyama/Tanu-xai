import { makeCacheableSignalKeyStore, type AuthenticationCreds, type AuthenticationState, type SignalDataSet, type SignalKeyStore } from '@whiskeysockets/baileys';
import { mkdir, writeFile } from 'node:fs/promises'; import { dirname, join } from 'node:path';
import { config } from '../config.js'; import { logger } from '../utils/logger.js';
export type SessionBundle = AuthenticationState & { saveCreds: (creds: Partial<AuthenticationCreds>) => Promise<void> };
function decodePayload(token: string): Record<string, unknown> { const encoded = token.slice('Tanu-XAI~'.length); for (const candidate of [encoded, encoded.replace(/-/g, '+').replace(/_/g, '/')]) { try { const value = JSON.parse(Buffer.from(candidate, 'base64').toString('utf8')); if (value && typeof value === 'object') return value; } catch { /* try next format */ } } throw new Error('SESSION_ID payload is not valid base64 JSON'); }
function objectKeyStore(input: Record<string, unknown>): SignalKeyStore { const data = new Map<string, unknown>(); for (const [type, values] of Object.entries(input)) if (values && typeof values === 'object') for (const [id, value] of Object.entries(values as Record<string, unknown>)) data.set(`${type}:${id}`, value); return { get: async (type, ids) => Object.fromEntries(ids.map(id => [id, data.get(`${type}:${id}`)]).filter(([, value]) => value !== undefined)) as never, set: async (updates: SignalDataSet) => { for (const [type, values] of Object.entries(updates)) for (const [id, value] of Object.entries(values ?? {})) { if (value === null) data.delete(`${type}:${id}`); else data.set(`${type}:${id}`, value); } }, clear: async () => data.clear() }; }
export function loadSession(): SessionBundle { if (!config.sessionId.startsWith('Tanu-XAI~')) throw new Error('Invalid SESSION_ID prefix'); const decoded = decodePayload(config.sessionId); const creds = (decoded.creds ?? decoded) as AuthenticationState['creds']; if (!creds || typeof creds !== 'object') throw new Error('SESSION_ID has no credentials'); let currentCreds = creds; const keys = objectKeyStore((decoded.keys ?? {}) as Record<string, unknown>); const authDir = join(process.cwd(), 'auth'); const saveCreds = async (next: Partial<AuthenticationCreds>) => { currentCreds = { ...currentCreds, ...next }; await mkdir(dirname(join(authDir, 'creds.json')), { recursive: true }); await writeFile(join(authDir, 'creds.json'), JSON.stringify(currentCreds), { mode: 0o600 }); }; logger.info('WA', 'Session decoded; sensitive values omitted'); return { creds, keys: makeCacheableSignalKeyStore(keys, undefined), saveCreds }; }
