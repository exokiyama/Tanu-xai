export interface DailyReportSchedulerOptions { enabled: boolean; intervalMs?: number; generate: () => Promise<string> | string; send: (body: string) => Promise<{ sent: boolean; reason?: string }>; onError?: (error: unknown) => void; }
export class DailyReportScheduler { private timer?: ReturnType<typeof setInterval>; private lastRun = ''; constructor(private readonly options: DailyReportSchedulerOptions) {} start() { if (!this.options.enabled || this.timer) return; const interval = this.options.intervalMs ?? 86_400_000; this.timer = setInterval(() => void this.run(), interval); } async run() { const date = new Date().toISOString().slice(0, 10); if (date === this.lastRun) return { sent: false, reason: 'already sent today' }; try { const body = await this.options.generate(); const result = await this.options.send(body); if (result.sent) this.lastRun = date; return result; } catch (error) { this.options.onError?.(error); return { sent: false, reason: error instanceof Error ? error.message : String(error) }; } } stop() { if (this.timer) clearInterval(this.timer); this.timer = undefined; } }
