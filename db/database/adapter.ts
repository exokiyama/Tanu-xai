export interface DatabaseAdapter { readonly name: string; readonly available: boolean; get<T = unknown>(table: string, filters: Record<string, string>): Promise<T | null>; upsert(table: string, value: Record<string, unknown>): Promise<void>; close(): Promise<void>; }
export class NullDatabaseAdapter implements DatabaseAdapter { readonly name = 'none'; readonly available = false; async get<T = unknown>() { return null as T | null; } async upsert() { throw new Error('Database is not configured; RPG/report persistence is disabled'); } async close() {} }
export class MemoryDatabaseAdapter implements DatabaseAdapter { readonly name = 'memory'; readonly available = true; private readonly data = new Map<string, Record<string, unknown>>(); async get<T>(table: string, filters: Record<string, string>) { const value = this.data.get(`${table}:${Object.values(filters).join(':')}`); return (value as T | undefined) ?? null; } async upsert(table: string, value: Record<string, unknown>) { const key = `${table}:${value.id ?? value.key ?? value.phone_number}`; this.data.set(key, value); } async close() { this.data.clear(); } }
