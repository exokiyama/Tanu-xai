'use strict';
const {getBalance,updateBalance}=require('../utils/rpg');
module.exports={pattern:'fight',aliases:['battle'],description:'Fight another user for a wager',category:'🎮 RPG',usage:'fight @user <amount>',async handler(sock,message,args,context){const target=message.quoted?.sender||context.mentionedJid?.[0];const amount=Number(args.find(x=>/^\d+$/.test(x)));if(!target||!amount||amount<=0)return context.reply('Usage: `.fight @user <amount>`');if(target===message.sender)return context.reply('❌ You cannot fight yourself.');const a=await getBalance(message.sender),b=await getBalance(target);if(a<amount||b<amount)return context.reply('❌ Both players need enough coins for the wager.');const winner=Math.random()<0.5?message.sender:target;const loser=winner===message.sender?target:message.sender;await updateBalance(loser,-amount,'fight_loss');await updateBalance(winner,amount,'fight_win');return context.reply(`⚔️ Fight complete!\n🏆 Winner: @${winner.split('@')[0]}\n🪙 Wager: ${amount}`,{mentions:[winner,loser]});}};
