'use strict';
const {query}=require('../database');
module.exports={pattern:'pet',aliases:['pets'],description:'View or adopt an RPG pet',category:'🎮 RPG',usage:'pet | pet buy <species> <name>',async handler(sock,message,args,context){const id=message.sender;if(args[0]==='buy'){const species=args[1]||'cat',name=args.slice(2).join(' ')||species;const r=await query('SELECT id FROM pets WHERE user_id=$1 LIMIT 1',[id]);if(r.rows.length)return context.reply('🐾 You already have a pet.');await query('INSERT INTO pets(user_id,name,species) VALUES($1,$2,$3)',[id,name,species]);return context.reply(`🐾 Adopted ${name} the ${species}!`);}const r=await query('SELECT name,species,hunger FROM pets WHERE user_id=$1 ORDER BY created_at DESC LIMIT 1',[id]);if(!r.rows[0])return context.reply('🐾 You have no pet. Use `.pet buy cat Luna`.');const p=r.rows[0];return context.reply(`🐾 *${p.name}*\nSpecies: ${p.species}\nHunger: ${p.hunger}/100`);}};
